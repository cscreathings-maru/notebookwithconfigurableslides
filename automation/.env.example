# Keys for the DEVELOPMENT loop (Aider). Separate from the app's runtime keys in deploy/.env.
# Copy to your code repo as `.env` (or export in your shell) before running aider.

# Claude — architect / planning / evaluation model
ANTHROPIC_API_KEY=sk-ant-...

# DeepSeek — editor / code-writing model
DEEPSEEK_API_KEY=sk-...

# Optional overrides
# AIDER_MODEL=anthropic/claude-sonnet-4-5
# AIDER_EDITOR_MODEL=deepseek/deepseek-chat
